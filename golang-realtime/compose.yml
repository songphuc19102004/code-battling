x-logging: &default-logging
  logging:
    driver: json-file
    options:
      max-size: 100M

services:
  server:
    image: codebatt
    ports:
      - "8082:8081"
    env_file: ./.env
    privileged: true
    <<: *default-logging
    restart: always
